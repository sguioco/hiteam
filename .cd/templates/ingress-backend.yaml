{{- if and .Values.api.enabled .Values.ingress.backend.enabled .Values.ingress.backend.host }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "hiteam.fullname" . }}-backend-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    argocd.argoproj.io/compare-options: IgnoreExtraneous
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    argocd.argoproj.io/sync-wave: "30"
spec:
  ingressClassName: {{ .Values.ingress.backend.className | quote }}
  tls:
    - hosts:
        - {{ .Values.ingress.backend.host | quote }}
      secretName: {{ include "hiteam.backendTlsSecret" . | quote }}
  rules:
    - host: {{ .Values.ingress.backend.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "hiteam.apiServiceName" . }}
                port:
                  number: {{ .Values.api.service.port }}
---
{{- if .Values.ingress.backend.enableHttpRedirect }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "hiteam.fullname" . }}-backend-ingress-http
  namespace: {{ .Release.Namespace }}
  annotations:
    argocd.argoproj.io/compare-options: IgnoreExtraneous
    traefik.ingress.kubernetes.io/router.entrypoints: web
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Release.Namespace }}-https-redirect@kubernetescrd"
    argocd.argoproj.io/sync-wave: "30"
spec:
  ingressClassName: {{ .Values.ingress.backend.className | quote }}
  rules:
    - host: {{ .Values.ingress.backend.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "hiteam.apiServiceName" . }}
                port:
                  number: {{ .Values.api.service.port }}
{{- end }}
{{- end }}
