Set up the OpenTelemetry Collector as a standalone Docker container with the following requirements:
- Install and run the collector binary as a Docker container
- Mount a configuration file from the host
- Expose necessary ports for telemetry reception and exporting
- Configure persistent storage for the configuration
- Set up restart policies and resource limits
